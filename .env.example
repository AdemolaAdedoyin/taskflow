NODE_ENV=development
PORT=4000

DATABASE_URL=postgresql://taskflow:taskflow@localhost:5432/taskflow
REDIS_URL=redis://localhost:6379

LOG_LEVEL=info

JOB_CONCURRENCY=10
JOB_DEFAULT_TIMEOUT_MS=30000

# Shared bearer token — every caller sends `Authorization: Bearer <this value>`
TASKFLOW_API_KEY=changeme-generate-a-long-random-value
